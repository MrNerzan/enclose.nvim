
Setting up with Packer:
```lua
 use({
        "mrnerzan/enclose.nvim",
        config = function()
            require("enclose").setup('v', 'r')
        end,
    })
 ```


 swap `r` with the desired keymap...
